BUGS:
- FIXED paint furniture has a bug, it renders furniture twice, light effects are correct though.
- raytracing for LoS has a bug where too many tiles are hidden
- wanderer npc does not keep to wandering box
- sound skips at startup
- FIXED music does stop and is completely restarted instead of pausing
- hit animation is not centered
- missile combat is possible with melee person
- recursive aStar does not work yet
- AStar for calculating moving towards player only returns space
- FIXED pressing space does not give NPCs a round
- player can move while hit animation is still running
- music switch between COMBAT and VICTORY back to standard does not work yet.
