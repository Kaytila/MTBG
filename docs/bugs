BUGS:
- paint furniture has a bug, it renders furniture twice, light effects are correct though. FIXED
- raytracing for LoS has a bug where too many tiles are hidden
- wanderer npc does not keep to wandering box
- sound skips at startup
- music does stop and is completely restarted instead of pausing
- hit animation is not centered
